<Page x:Class="XHair.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="SettingsPage">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0">
            <Label Name="settingsLabel" Content="Settings" HorizontalAlignment="Center" FontSize="20" Margin="0,0,0,20" />
            
            <Border BorderBrush="Black" BorderThickness="2" CornerRadius="5" Padding="5">
                <StackPanel>
                    <Button Name="HideKey" Content="Set hide key" FontSize="15" Click="SetHideKey_Click" Margin="0,0,0,0"/>                    
                </StackPanel>
            </Border>

            <Border BorderBrush="Black" BorderThickness="2" CornerRadius="5" Padding="5">
                <StackPanel>
                    <Button Name="userPath" Content="Custom folder" FontSize="15" Click="UserPath_Click" Margin="0,0,0,0"/>
                    <Label Name="userPathLabel" Content="" HorizontalAlignment="Center" FontSize="15"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>
